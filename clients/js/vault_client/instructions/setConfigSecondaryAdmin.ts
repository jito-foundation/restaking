/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type TransactionSigner,
} from '@solana/web3.js';
import { JITO_VAULT_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getConfigAdminRoleDecoder,
  getConfigAdminRoleEncoder,
  type ConfigAdminRole,
  type ConfigAdminRoleArgs,
} from '../types';

export const SET_CONFIG_SECONDARY_ADMIN_DISCRIMINATOR = 33;

export function getSetConfigSecondaryAdminDiscriminatorBytes() {
  return getU8Encoder().encode(SET_CONFIG_SECONDARY_ADMIN_DISCRIMINATOR);
}

export type SetConfigSecondaryAdminInstruction<
  TProgram extends string = typeof JITO_VAULT_PROGRAM_ADDRESS,
  TAccountConfig extends string | IAccountMeta<string> = string,
  TAccountAdmin extends string | IAccountMeta<string> = string,
  TAccountNewAdmin extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            IAccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountNewAdmin extends string
        ? ReadonlyAccount<TAccountNewAdmin>
        : TAccountNewAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type SetConfigSecondaryAdminInstructionData = {
  discriminator: number;
  configAdminRole: ConfigAdminRole;
};

export type SetConfigSecondaryAdminInstructionDataArgs = {
  configAdminRole: ConfigAdminRoleArgs;
};

export function getSetConfigSecondaryAdminInstructionDataEncoder(): Encoder<SetConfigSecondaryAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['configAdminRole', getConfigAdminRoleEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_CONFIG_SECONDARY_ADMIN_DISCRIMINATOR,
    })
  );
}

export function getSetConfigSecondaryAdminInstructionDataDecoder(): Decoder<SetConfigSecondaryAdminInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['configAdminRole', getConfigAdminRoleDecoder()],
  ]);
}

export function getSetConfigSecondaryAdminInstructionDataCodec(): Codec<
  SetConfigSecondaryAdminInstructionDataArgs,
  SetConfigSecondaryAdminInstructionData
> {
  return combineCodec(
    getSetConfigSecondaryAdminInstructionDataEncoder(),
    getSetConfigSecondaryAdminInstructionDataDecoder()
  );
}

export type SetConfigSecondaryAdminInput<
  TAccountConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountNewAdmin extends string = string,
> = {
  config: Address<TAccountConfig>;
  admin: TransactionSigner<TAccountAdmin>;
  newAdmin: Address<TAccountNewAdmin>;
  configAdminRole: SetConfigSecondaryAdminInstructionDataArgs['configAdminRole'];
};

export function getSetConfigSecondaryAdminInstruction<
  TAccountConfig extends string,
  TAccountAdmin extends string,
  TAccountNewAdmin extends string,
  TProgramAddress extends Address = typeof JITO_VAULT_PROGRAM_ADDRESS,
>(
  input: SetConfigSecondaryAdminInput<
    TAccountConfig,
    TAccountAdmin,
    TAccountNewAdmin
  >,
  config?: { programAddress?: TProgramAddress }
): SetConfigSecondaryAdminInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountAdmin,
  TAccountNewAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? JITO_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: false },
    newAdmin: { value: input.newAdmin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.newAdmin),
    ],
    programAddress,
    data: getSetConfigSecondaryAdminInstructionDataEncoder().encode(
      args as SetConfigSecondaryAdminInstructionDataArgs
    ),
  } as SetConfigSecondaryAdminInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountNewAdmin
  >;

  return instruction;
}

export type ParsedSetConfigSecondaryAdminInstruction<
  TProgram extends string = typeof JITO_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    admin: TAccountMetas[1];
    newAdmin: TAccountMetas[2];
  };
  data: SetConfigSecondaryAdminInstructionData;
};

export function parseSetConfigSecondaryAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedSetConfigSecondaryAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      admin: getNextAccount(),
      newAdmin: getNextAccount(),
    },
    data: getSetConfigSecondaryAdminInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
